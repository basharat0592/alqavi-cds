
You are improving an existing Al-Qavi Online Shop Management System that was built from a previous prompt. The database, folder structure, admin panel, and storefront already exist. Do NOT rebuild from scratch. Only add, modify, or extend what is described below.

---

## CONTEXT OF EXISTING SYSTEM

The existing system has:
- Django backend with models: User, Supplier, Category, Product, PurchaseOrder, PurchaseOrderItem, Inventory, StockAdjustment, Warehouse, Order, OrderItem, NavbarPage
- Admin panel with pages: Dashboard, Products, Categories, Suppliers, Purchase Orders, Inventory, Orders, Customers, Settings
- User storefront with pages: Home, Shop by Category, Product Detail, Cart, Checkout, Order Tracking, Account

---

## IMPROVEMENT 1 — PRODUCT PRICING (Most Important)

### Modify the Product model to add these fields:

```python
cost_price       # FloatField — price we paid supplier (hidden from customer)
selling_price    # FloatField — price customer sees
compare_price    # FloatField (nullable) — original/crossed-out price on storefront
profit_amount    # Property — auto calculated: selling_price - cost_price
profit_margin    # Property — auto calculated: (profit_amount / cost_price) * 100
```

### Auto-fill cost_price from Purchase Order:
When a PurchaseOrder is marked as RECEIVED, for each PurchaseOrderItem:
- Set product.cost_price = item.unit_cost (if not already set or if new cost is different)
- Log the cost change in a new CostHistory model

### Admin Add/Edit Product form — show these pricing fields:
```
PRICING SECTION
├── Cost Price (Rs.)          ← prefilled from last PO, editable
├── Selling Price (Rs.)       ← admin sets this manually
├── Compare Price (Rs.)       ← optional, shows as strikethrough on storefront
├── Profit Amount             ← READ ONLY, auto-calculated live
├── Profit Margin %           ← READ ONLY, auto-calculated live, color coded:
│     green  if margin >= 40%
│     amber  if margin 20-39%
│     red    if margin < 20%
└── Tax Rate %                ← optional
```

Use JavaScript on the admin product form to calculate profit_amount and profit_margin live as admin types selling_price.

### Customer storefront — show ONLY:
- selling_price (as main price)
- compare_price (as strikethrough if set and > selling_price)
- NEVER show cost_price, profit_amount, or profit_margin to customer

---

## IMPROVEMENT 2 — SUPPLIER MODEL ENHANCEMENTS

### Add these fields to Supplier model:
```python
company_name        # CharField
contact_person      # CharField
phone               # CharField
whatsapp            # CharField (nullable)
email               # EmailField
address             # TextField
city                # CharField
country             # CharField (default: Pakistan)
payment_terms       # CharField choices: CASH, CREDIT_7, CREDIT_15, CREDIT_30
credit_limit        # FloatField (nullable)
outstanding_balance # FloatField (default 0)
notes               # TextField (nullable)
status              # CharField: ACTIVE / INACTIVE
total_orders        # Property — count of all POs for this supplier
total_spent         # Property — sum of all received POs for this supplier
```

### Supplier detail page in admin:
Build a supplier detail page `/admin/purchasing/suppliers/[id]` that shows:
- Full supplier info card
- All purchase orders from this supplier (table)
- Total spent with this supplier
- Outstanding balance tracker
- Quick "New Purchase Order" button pre-filled with this supplier

---

## IMPROVEMENT 3 — PURCHASE ORDER ENHANCEMENTS

### Add these fields to PurchaseOrder model:
```python
invoice_number      # CharField (nullable) — supplier's invoice ref
invoice_date        # DateField (nullable)
payment_status      # CharField: UNPAID / PARTIAL / PAID
amount_paid         # FloatField (default 0)
amount_remaining    # Property — total_cost - amount_paid
freight_cost        # FloatField (default 0)
tax_amount          # FloatField (default 0)
grand_total         # Property — total_cost + freight_cost + tax_amount
received_by         # ForeignKey to User (nullable)
received_at         # DateTimeField (nullable)
```

### Add these fields to PurchaseOrderItem model:
```python
batch_number        # CharField (nullable)
expiry_date         # DateField (nullable) — important for cosmetics
bonus_quantity      # IntegerField (default 0) — free items from supplier
total_quantity      # Property — quantity + bonus_quantity
```

### When PO status is set to RECEIVED:
1. For each item: increment Inventory.quantity by item.total_quantity
2. For each item: update product.cost_price = item.unit_cost
3. Set po.received_at = now()
4. Set po.received_by = current admin user
5. Create StockAdjustment record for audit trail
6. Update supplier.outstanding_balance += po.amount_remaining

---

## IMPROVEMENT 4 — COSMETICS-SPECIFIC PRODUCT FIELDS

### Add these fields to Product model:
```python
brand               # CharField (nullable)
volume_weight       # CharField (nullable) — e.g. "50ml", "100g", "200ml"
skin_type           # CharField choices: ALL, DRY, OILY, COMBINATION, SENSITIVE
key_ingredients     # TextField (nullable)
expiry_date         # DateField (nullable)
country_of_origin   # CharField (nullable)
shade_color         # CharField (nullable) — for foundations, lipsticks etc
```

### Show these on storefront product detail page:
- Volume/Weight as a badge next to product name
- Skin Type as a tag
- Key Ingredients as expandable section
- Shade/Color as color swatch if set

---

## IMPROVEMENT 5 — ADMIN DASHBOARD UPGRADES

### Add these stat cards to existing dashboard:
- Total Profit This Month (sum of all orders: selling_price - cost_price)
- Average Profit Margin % across all products
- Products with margin below 20% (red alert count)
- Outstanding Supplier Payments (sum of all unpaid PO balances)

### Add a Profit Overview section on dashboard:
A table showing top 10 products by profit margin with columns:
Product | Cost Price | Selling Price | Profit | Margin % | Units Sold

### Low stock panel:
Already exists — make sure it shows: Product | Current Stock | Last Purchase Price | Selling Price

---

## IMPROVEMENT 6 — ADMIN PRODUCT TABLE UPGRADES

### In the All Products table, add these columns:
- Cost Price (visible only to admin, hidden from all customer-facing APIs)
- Selling Price
- Profit Margin % (color coded badge: green/amber/red)
- Stock

### Add a quick-edit inline feature:
Clicking the selling price in the table opens an inline input to update it without opening full edit form. On save, recalculate and update profit_margin badge live.

---

## IMPROVEMENT 7 — STOREFRONT CATEGORY FLOW (Fix & Verify)

Ensure this flow works correctly end to end:
1. Admin creates a Category and assigns it to a NavbarPage
2. Admin adds a Product and assigns it to that Category
3. On the storefront, NavbarPage appears in the top navbar
4. Clicking that navbar item shows all ACTIVE products in all categories assigned to that navbar page
5. Product shows selling_price and compare_price (never cost_price)

If any part of this flow is broken in the existing code, fix it.

---

## IMPROVEMENT 8 — API SECURITY

Ensure these rules are enforced in ALL API endpoints:
- cost_price, profit_amount, profit_margin are NEVER returned in any customer-facing API response
- These fields only appear in admin-authenticated endpoints
- Add a ProductAdminSerializer and ProductPublicSerializer — use public serializer for all storefront endpoints

---

## MIGRATION INSTRUCTIONS

After making model changes:
1. Run: python manage.py makemigrations
2. Run: python manage.py migrate
3. For existing products with no cost_price, set cost_price = selling_price * 0.6 as a default (60% assumed cost)
4. Write a data migration for this default

---

## DO NOT CHANGE

- Existing URL structure
- Existing auth system
- Existing order flow
- Existing warehouse/inventory structure
- Any working feature from the original system

Only ADD new fields, EXTEND existing forms, and FIX the category→storefront flow if broken.
