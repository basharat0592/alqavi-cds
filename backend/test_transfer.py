import requests

URL = "http://127.0.0.1:8000/api/v1/inventory/transfers/"
# I don't have a token, but let's assume I can get one or the user is logged in.
# Wait, I should probably check the logs of the running server.
# But I can't easily see the server logs in real-time unless I read the terminal.
